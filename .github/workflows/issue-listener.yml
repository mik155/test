name: Listen issu
on: 
  workflow_dispatch:
  issues:
    types: [opened]
jobs:
  print-context:
    runs-on: ubuntu-latest
    steps:
      - name: print context
        run: | 
          echo "${{ github.event.issue.title }}"
          echo "${{ github.event.issue.body }}"
